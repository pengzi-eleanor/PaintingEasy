# PaintingEasy Agent Harness

## 项目目标

PaintingEasy 是面向创作者的素材搜索辅助插件，目标是在一个入口中完成：

1. 多图库间跳转搜索；
2. 搜索关键词的补全、纠错、扩展与智能优化；
3. 根据自然语言描述，由 AI 提供可编辑、可选用的搜索关键词；
4. 图片语义识别、图片关键词提取和以图搜图。

产品首先解决“需要在多个图库反复搜索、耗时长、结果精度不足”的问题。实现时应保持插件端轻量，把图库适配、聚合、AI 调用和向量检索放在后端服务中。

## 需求与优先级

按以下顺序推进，除非用户明确调整优先级：

1. **MVP：多平台素材搜索辅助插件**
  - 插件提供统一搜索入口，支持将用户的自然语言素材需求转化为结构化中英文检索词，并允许用户一键跳转到多个素材平台搜索。
  - 第一版不直接聚合图库 API 结果，而是通过关键词优化和多平台搜索分发降低用户在多个素材网站之间反复试错的成本。
2. **智能关键词优化**
   - 在线关键词扩展以腾讯词向量（Tencent AI Lab Embedding Corpus for Chinese Words and Phrases）为主要数据源，以腾讯云翻译API为辅助，暂不要求本地训练 Embedding、维护永久向量库或以有限手工词库承担默认召回；
   - SQLite 仅缓存外部知识源的标准化候选和状态，不保存完整用户 query，也不直接视为永久本地知识库；只有结合脱敏统计、人工审核和版本流程后，候选才可离线固化；
   - 保留原始查询，不得在没有提示的情况下改变用户意图；
   - 建议词应可单独选择、删除或回退。
3. **个性化关键词优化**
   - 给出常见的用户群体分类，根据分类进行关键词优化（加入对应角色的固定描述），在加入AI功能后在提示词中加入对应角色与设定故事环境；
   - 将给出的搜索词按照原语义与衍生搜索进行分类，即在给出推荐关键词的基础上保留原语义，并根据不同用户身份、不同搜索平台给出对应的关键词建议排序，提供主体/场景/颜色建议；
   - 对不同平台的query参数进行协调适配，专精化提供对应关键词；
   - 不同平台擅长风格进行建模存储，并在搜索时按照对应修改即可
4. **AI 描述转关键词**
   - 主要处理长文本和模糊创作描述；
   - 首选 Gemini API 或其他具有可用额度的多模态 API，本地可替换为 Qwen-VL/Qwen2.5-VL；
   - 用户先选择身份，再明确选择“基础优化”或“智能优化”；基础优化不消耗大模型额度，智能优化才调用大模型；
   - AI 不可用或额度不足时回退基础优化；外部知识源也不可用且无可用缓存时，仍须保留原始 query 完成平台跳转。
5. **图片语义识别**
   - 使用多模态模型从上传图片提取对象、场景、风格、色彩、构图等可搜索语义；
   - 输出结构化、可编辑的关键词，并清楚标示模型生成内容；
   - 优先考虑 Qwen2.5-VL API；供应商实现必须可替换。

## 预期技术栈与边界

- 前端：Vue 3、TypeScript、Vite、Element Plus、Pinia、Axios、CSS。
- 后端：Python 3.11+、FastAPI、Pydantic、httpx、SQLite、SQLAlchemy。
- AI/检索：OpenCLIP、PyTorch、FAISS，以及可替换的多模态模型供应商。
- 前后端通过版本化 HTTP API 通信；不要让前端依赖某个图库或 AI 供应商的原始响应结构。
- 图库连接器、关键词提供器、视觉模型和向量存储都应通过清晰接口隔离，便于替换与测试。
- 默认只保存完成业务所需的数据。用户上传图片、API 密钥、下载链接和模型响应均视为敏感数据。

建议的顶层结构如下；在项目尚未生成对应模块时，不要为了匹配目录而创建空文件：

```text
frontend/              Vue 插件界面
backend/app/api/       FastAPI 路由与请求/响应模型
backend/app/services/  搜索编排、排序、关键词与视觉服务
backend/app/providers/ 图库和 AI 供应商适配器
backend/app/models/    数据库模型与领域模型
backend/tests/         后端测试
docs/                  架构、接口和决策记录
```

## 强制修改流程

对仓库进行任何修改时，必须严格执行以下流程：

1. **修改前先完整阅读根目录的 `CHANGELOG.md`**，检查既有决策、失败尝试、兼容性问题和待办事项，避免重复踩坑。即使本次修改看似无关，也不能跳过。
2. 阅读与任务相关的源码、测试、配置和文档；不要依据文件名猜测现状。
3. 先做满足需求的最小闭环修改，避免无关重构和提前引入沉重依赖。
4. 执行与变更风险相称的格式检查、静态检查、单元测试和集成测试。
5. **每次修改完成后，必须在 `CHANGELOG.md` 顶部追加一条简短记录**。记录日期、修改内容、验证方式，以及值得后续注意的坑或未解决问题。

`CHANGELOG.md` 是开发记忆，不是发布宣传。记录应简短、具体、可操作；不要写入密钥、令牌、个人数据、完整模型输入或其他敏感信息。如果一次任务包含多次连续修正，可以在交付前合并成一条准确记录，但不能遗漏最终验证结果。

## 开发约束

- 先定义统一领域模型，再接入图库；供应商特有字段只能留在适配层。
- 外部请求必须设置超时、有限重试、并发上限和错误映射；不得无限等待或静默吞错。
- 聚合结果的去重优先使用来源 ID、规范化 URL、感知哈希或内容哈希；仅以标题去重不可靠。
- 排序逻辑应可测试、可解释。将文本相关性、视觉相似度、质量和来源权重分开计算。
- API 输入输出使用 Pydantic 模型并保持向后兼容；破坏性变更需版本化并记录。
- 所有外部服务通过依赖注入或协议接口替换，测试中不得真实消耗付费 AI/API 配额。
- API 密钥只从环境变量或密钥管理系统读取；提供 `.env.example` 时只允许占位值。
- 上传图片必须限制类型与大小，验证真实内容，使用随机临时文件名，并在处理后清理。
- 尊重图库许可、署名、速率限制和使用条款；展示结果时保留来源及许可信息。
- 日志中不得输出密钥、完整图片、敏感 URL 参数或不必要的用户查询内容。
- 用户可见 AI 结果要注明为建议，并允许编辑、拒绝和回退。

## 搜索模式与知识扩展

- 文本搜索固定流程为“选择身份 → 选择搜索方式 → 输入或确认需求 → 生成可编辑关键词和平台方案”。旧请求未提供搜索方式时，为保持兼容按 `basic` 处理。
- `basic` 基础优化使用输入规范化、本地腾讯中文词向量、职业固定规则和平台固定规则，不调用大模型；不得将基础优化描述为 AI 生成。
- `smart` 智能优化在基础候选上构建结构化 Prompt，并要求大模型输出经严格模型校验的核心意图、分类关键词、衍生依据、不确定项和平台优先顺序。平台只能从配置白名单选择，URL 必须由后端连接器生成，不能相信模型直接输出的 URL。
- 大模型不得虚构用户未提到的重要主体；核心词最多 6 个、扩展词最多 8 个、推荐平台最多 5 个。非核心候选必须包含原因和 `derived_from`。
- 智能优化失败时回退基础优化；不得因为 AI 额度、超时或结构错误导致基础搜索失效。
- 不接入 Wikidata、ConceptNet 或有限手工词库；腾讯中文词向量是当前唯一关键词知识库，模型缺失或无候选时保留原始 query。
- 轻量缓存使用有界、带 TTL 的进程内实现，只保存查询摘要键和标准化结果，不能充当永久知识库。
- 翻译、大模型与图片语义识别必须保留独立 Provider 接口，并通过依赖注入接入；外部请求统一具备超时、有限重试和脱敏错误降级，不得删除通用外部 API 基础设施。

## 职业平台规则与版权提示

- 平面设计师默认推荐顺序初稿为：花瓣网、小红书、Pinterest、Pixso、创客贴、Behance、Freepik。重点覆盖版式、品牌视觉、海报、配色、字体搭配、营销设计和案例参考。
- 插画师默认推荐顺序初稿为：Pinterest、Pixiv、ArtStation、Behance、花瓣网、Freepik、小红书。
- 摄影师默认推荐顺序初稿为：Unsplash、Pexels、视觉中国、Pixabay、Pinterest、小红书。
- 电商工作者默认推荐顺序初稿为：创客贴、Freepik、视觉中国、Pexels、小红书、花瓣网、Pinterest。
- UI 设计师默认推荐顺序初稿为：Figma Community、Pixso、Dribbble、Behance、Pinterest、花瓣网、Freepik。
- 上述名单和顺序属于待用户审核的产品规则，应配置化、版本化，不得散落硬编码；用户确认前允许调整，不应宣称效果已经验证。
- 每个平台必须配置版权状态，至少支持 `inspiration_only`、`license_per_item`、`attribution_required`、`broad_reuse_license`、`commercial_license` 和 `unknown`。花瓣网、小红书、Pinterest、Behance、Dribbble、ArtStation、Pixiv 默认只作为灵感或案例参考，不得暗示搜索结果可直接商用。
- 对许可随资源、账号或订阅变化的平台，必须提示用户逐项查看原始许可。即使平台提供宽松许可，也要提示肖像、商标、艺术品、背书、未修改转售和再分发等额外限制。

## 外部 API 联网门禁

- 翻译、大模型与图片语义识别供应商应在离线 mock、缓存、超时、重试、降级和前端交互测试完成后再启用真实网络。
- 真实联网不得绕过网络或合规限制；目标环境异常时必须保留真实失败证据并回退基础搜索，不得用 Mock 结果声称连接成功。
- 启用具体供应商前，界面必须说明会发送的数据范围并允许用户控制相关能力；完整历史、无关图片、密钥和其他敏感数据不得发送。

## 测试与验收

新增行为必须配套测试。最低验收要求：

- 前端：类型检查、组件/状态测试；关键搜索流程应覆盖加载、空结果、部分失败和重试状态。
- 后端：格式/静态检查、单元测试；提供器使用 mock 或录制的脱敏 fixture。
- 聚合搜索：覆盖字段标准化、跨源去重、稳定排序、超时、限流和部分成功。
- 关键词能力：覆盖不改变原意、重复词清理、中英文输入、空输入和供应商降级。
- 图片能力：覆盖非法类型、超限文件、模型失败、临时文件清理，以及固定向量的可重复排序。
- 修复缺陷时，先添加能复现问题的回归测试，或在 `CHANGELOG.md` 中说明无法添加的原因。

在没有成熟命令前，先依据实际项目配置选择命令，不要虚构脚本。命令稳定后，应在此处补充唯一的标准验证入口。

## 完成定义

任务只有在以上条件全部满足时才算完成：需求形成可使用的闭环；错误和降级路径明确；相关测试已通过；文档/API 契约同步；没有提交密钥或敏感数据；已在交付前更新 `CHANGELOG.md`，且记录了实际执行的验证和遗留风险。
