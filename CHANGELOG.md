# PaintingEasy 修改记录

## 2026-09-10 — 补充本地文档与依赖环境忽略规则

- 修改：`.gitignore` 新增需求文档、Python 虚拟环境、安装产物和 `*.egg-info` 忽略规则；保留 `pyproject.toml`、`package.json`、`package-lock.json` 等依赖版本文件可提交。
- 验证：核对忽略规则与仓库现有依赖版本文件路径；未运行代码检查（本次仅修改 Git 忽略配置和开发记录）。

## 2026-09-04 — 最终验收知识库与响应模型

- 修改：完成五种职业规则、六平台共享配置、代表性关键词分类、候选分组/溯源、平台独立 query/URL、确定性评分和旧字段兼容视图验收；修正默认 Embedding provider 为未启用，默认明确返回 `rules_only`，避免将 Mock 误报为向量检索。未接入真实 Embedding、LLM、FAISS 或外部 API，未发现密钥或敏感日志。
- 验证：职业/平台/分类审计通过；平台测试 3 项通过；服务冒烟验证通过；前端 `npm run type-check` 通过；Ruff 执行但仓库存在 105 项既有格式/导入告警；完整后端 pytest 因环境缺少 `PIL` 无法收集；前端 Vitest 因无法写入 `extension/node_modules/.vite-temp` 无法启动。
- 未解决：后端完整 API/图片测试和前端组件测试仍需可安装 Pillow 且允许 Vite 临时目录写入的环境复核。下一步接入真实 Embedding 前需确定模型/provider 配置、超时配额、脱敏 fixture、向量版本迁移和稳定排序基线。

## 2026-09-04 — 增加检索元信息和兼容层

- 修改：新增 `RetrievalMeta` 与新搜索响应字段，明确 rules-only、hybrid、fallback 策略、Embedding 状态、知识库版本、候选数和降级含义；旧 `query`、`suggestions`、`platforms` 保留为兼容视图并由新候选结果生成。同步前端类型与 API 示例，预留 v2 迁移空间。
- 验证：后端平台测试 3 项通过；直接服务冒烟验证通过；前端 `npm run type-check` 通过。完整后端 pytest 仍受环境缺少 `PIL` 依赖影响，Vitest/生产构建仍受 `.vite-temp` 写入限制影响。未返回 Prompt、向量内容或供应商异常详情。

## 2026-09-04 — 实现平台独立响应

- 修改：新增 `PlatformQuery` 领域模型，返回平台独立 terms、各分组候选 ID、登录状态、确定性评分和采用规则原因。平台 query 不再直接拼接全部候选词；核心词按选中状态保留，平台规则隔离，URL 从最终 terms 重新生成。同步前端平台类型并保留现有界面布局。
- 验证：平台配置测试与前端类型检查待本次执行；完整后端 pytest 可能受当前环境缺少 `PIL` 依赖影响，Vitest/构建可能受 `.vite-temp` 写入限制影响。未调用外部图库或 AI 服务。

## 2026-09-04 — 重构关键词响应领域模型

- 修改：新增经范围校验的 `ScoreBreakdown` 与确定性加权 `final_score`，新增带稳定 ID、分组、来源、溯源和平台适用范围的 `KeywordCandidate`；文本与图片响应统一使用新候选模型。前端删除操作改用 `id`，保留旧测试数据的类型兼容，不改变界面布局；同步 API 文档。
- 验证：前端 `npm run type-check` 通过；新增平台模型测试 3 项通过。完整后端 pytest 因当前环境缺少 `PIL` 依赖未完成收集；Vitest/生产构建因受限环境无法写入 `.vite-temp` 未启动。未暴露供应商响应、Prompt 或密钥。

## 2026-09-04 — 统一六个平台配置与 query 规则

- 修改：新增 `shared/platforms.json` 作为文本搜索、图片搜索和推荐平台共用配置源，覆盖 Unsplash、Pexels、Pixabay、Freepik、视觉中国和花瓣网；补齐平台能力、语言、规则原因、query 策略、登录标记、启用状态和版本。后端按最终 terms 独立生成平台 query 与 URL，保留核心词并限制词数；前端图片平台、文本搜索、关键词删除后的链接均改为从配置重建 URL。
- 验证：`extension` 的 `npm run type-check` 通过；新增平台配置单元测试并更新六平台 API 断言。后端 pytest 因当前环境缺少 `PIL` 依赖未完成收集；Vitest 因受限环境无法写入 `extension/node_modules/.vite-temp` 未启动。未调用外部图库或 AI 服务。

## 2026-09-04 — 补齐结构化职业排序规则

- 修改：新增严格校验的职业配置和独立规则引擎，覆盖平面设计师、插画师、摄影师、电商工作者与 UI 设计师的双语名称、目标、分类权重、条件修饰词、禁止扩展、平台优先级、提示上下文和版本。职业仅对候选词做可解释稳定排序；修饰词必须命中显式条件，UI 扩展额外要求 UI 意图，关系禁用不影响直接匹配主体，未选择职业时不生成职业词。关键词优化服务未硬编码职业规则，未接入 LLM 或外部服务。
- 验证：后端完整 pytest 通过（36 tests，1 条既有 Starlette 弃用警告）；本次涉及文件 Ruff 检查通过。全仓既有 Ruff 告警未在本次扩大范围处理。

## 2026-09-04 — 结构化关键词知识库

- 修改：以严格 Pydantic 模型实现 `KeywordConcept`、双语标准词/别名/检索词和带权概念关系，使用稳定 ID、版本、启停及适用范围字段；七个主要分类均补充多条示例。检索改为仅消费校验后的知识库，按概念 ID 去重、忽略禁用项，并禁止关系扩展引入未直接匹配的主体。
- 验证：后端完整 pytest 通过（26 tests，1 条既有 Starlette 弃用警告）；本次涉及文件 Ruff 检查通过。全仓 Ruff 仍有 108 项既有格式/导入告警，集中在旧 API、provider、分析脚本和遗留 `appservicesag.py`；未引入数据库、FAISS、真实 Embedding 或外部 API。

## 2026-09-04 — 扩展识图搜索平台与职业推荐

- 修改：识图搜索平台由 2 个扩展至 Unsplash、Pexels、Pixabay、Freepik、视觉中国和花瓣网，对需登录站点明确标示；识图页底部新增按现有五类职业切换的网站推荐栏，未选择职业时展示通用推荐。未修改图片识别、关键词筛选和文本搜索逻辑。
- 验证：前端类型检查通过；16 个 Vitest 测试通过；生产构建通过。构建仍有既有主包大于 500 kB 警告；视觉中国与花瓣网的登录状态由目标网站自行处理。

## 2026-09-03 — 统一文本与识图关键词结果界面

- 修改：移除文本关键词结果的重复卡片嵌套和图片页重复的识别按钮；文本与识图统一复用同一关键词编辑、筛选、平台单选及搜索结果组件，图片删除关键词时同步更新完整关键词。
- 验证：前端类型检查通过；14 个 Vitest 测试通过；生产构建通过。构建仍有既有主包大于 500 kB 警告。

## 2026-09-03 — 复用文本与识图关键词选择组件

- 修改：抽取共享关键词选择组件，文本优化与图片识图统一复用完整关键词框、关键词多选、平台单选和搜索按钮；图片识别结果支持删除、编辑和按选中词跳转。
- 验证：待执行前端类型检查；未新增外部依赖。

## 2026-09-03 — 修复上传目录不存在导致的启动错误

- 修改：应用启动阶段主动创建图片上传目录后再挂载静态文件服务，避免 `backend/uploads` 不存在时触发运行时错误。
- 验证：已完成 Python 语法级检查；建议重启后端后访问 `/health` 与图片上传接口。

## 2026-09-03 — 修复上传目录启动错误

- 修改：FastAPI 启动时主动创建图片上传目录，再挂载静态文件服务，避免 `backend/uploads` 不存在导致应用启动失败。
- 验证：待执行后端启动与接口测试。

## 2026-09-03 — 增加关键词多选与平台单选搜索

- 修改：平台改为单选，关键词保留完整文本供复制并支持多选；新增底部搜索按钮，跳转仅使用勾选关键词和所选平台，删除关键词同步更新完整文本。
- 验证：待执行前端类型检查与测试。

## 2026-09-03 — 增加请求超时与图片重复上传处理

- 修改：前端请求 15 秒无响应自动结束并显示中文提示，移除错误分类英文前缀；图片选择框每次点击前清空自身值，清空后可再次选择同一文件上传。
- 验证：待执行前端类型检查；后端接口未变更。

## 2026-09-03 — 增加图片职业与平台适配

- 修改：图片区域改为自适应内边距卡片，右下角使用红色“清空”按钮；图片分析接口接收 `persona` 与 `platform`，从本地 JSON 规则组合职业和平台专属关键词，保留对象语义词优先顺序；未来 AI provider 可直接复用结构化上下文。
- 验证：待执行前端类型检查；未引入数据库或真实 AI。

## 2026-09-03 — 修正图片缩略图比例

- 修改：缩略图不再使用固定宽高，改为最大宽高约束并保持 `width/height: auto`，根据图片方向缩放且不改变原始比例。
- 验证：CSS 静态调整；前端类型契约未变化。

## 2026-09-03 — 优化图片预览、手动识别与关键词规则

- 修改：上传后不再自动分析；新增固定尺寸缩略图与屏幕约束原图弹窗；图片关键词优先返回对象语义，并通过本地 JSON 增加平台专属建议与分类，未命中时保留 Mock 降级词；挂载上传静态访问目录。
- 验证：前端类型检查待执行；未接入数据库或真实 AI。

## 2026-09-03 — 增加图片上传与语义识别预留链路

- 修改：新增本地临时图片上传、Pillow 格式/可解析性/大小校验、元数据与过期清理脚本；抽象 `ImageStorageProvider`、`ImageAnalyzeProvider` 并接入稳定 Mock 分析接口；前端支持预览、上传、删除、重新上传和识别按钮。未接入真实 AI API、云存储或以图搜图。
- 验证：待执行后端 pytest、前端类型检查；遗留风险：元数据当前为进程内存，服务重启后历史临时图片不可恢复。

## 2026-09-03 — 增加轻量本地数据埋点

- 修改：新增 `AnalyticsService`、`AnalyticsProvider`、JSONL/Noop provider、埋点上报接口与统计脚本；前端接入搜索、建议生成、图片上传/识别等关键行为，默认脱敏 query。
- 验证：后端 `python -m pytest -q -p no:cacheprovider` 通过（19 tests）；`extension` 的 `npm run type-check` 通过；Vitest 因受限环境无法写入 `extension/node_modules/.vite-temp` 未启动；统计脚本样例运行通过。当前未接入数据库、第三方分析平台或可视化。

## 2026-09-03 — 完善角色选项与关键词联动删除

- 修改：角色提示精简为“您的身份是：”，扩展为五类创作者角色；关键词卡片改为自动换行一次展示，关闭卡片时同步删除建议、最终 query 和平台链接中的对应词。
- 验证：`npm run type-check`、11 个 Vitest 测试和 `npm run build` 通过；构建仅保留既有主包大于 500 kB 警告。未接入埋点、外部 API 或新依赖。遗留风险：新增三类角色当前可透传但尚无专属后端规则，删除统计需后续埋点接入。

## 2026-09-03 — 优化角色选择与关键词标签页交互

- 修改：文本搜索区新增可选用户角色选择框并将 `persona` 传给后端；隐藏原始 query 展示，关键词改为可关闭的标签页，每个标签页展示来源和分类信息。
- 验证：`npm run type-check` 通过；Vitest 因受限环境无法写入 `extension/node_modules/.vite-temp` 未启动。未接入外部 API。遗留风险：角色当前为固定两项，平台选择仍沿用既有设置流程。

## 2026-09-02 — 建立离线 RAG-ready 关键词优化骨架

- 修改：新增可维护关键词知识库、Embedding/LLM 协议与确定性 Mock、规则/别名检索、受控上下文、结构化输出校验和 provider 降级链路；搜索请求增加可选 `persona`/`platforms`，同步前端调用和 API 文档。
- 验证：后端 `python -m pytest -q -p no:cacheprovider` 通过（19 tests）；前端 `npm run type-check` 通过；Ruff 使用 `--no-cache` 检查时受限环境无法写入缓存且现有长行规则仍有告警。未调用外部 API 或本地模型。遗留风险：当前检索仍为规则/别名匹配，真实 Embedding/LLM provider 尚未实现，平台 query 模板尚未做平台级语义改写。

## 2026-08-31 — 修正后端联调入口与跨域配置

- 修改：确认关键词接口为 `POST /api/v1/search/assist`，根路径新增服务说明；开发期启用 CORS，允许前端扩展调用 API；同步接口文档和验证用例。
- 验证：新增根路径与 CORS 预检测试；未调用真实 AI、图库或付费 API。遗留风险：生产环境需将通配 CORS 收紧为实际扩展来源。

## 2026-08-27 — 补齐前端搜索与状态管理自动化测试

- 修改：复用 Vitest、Vue Test Utils 和 jsdom，新增搜索 API mock 边界测试、Pinia 搜索状态测试，并覆盖空输入约束、成功/空结果、错误分类、重试可调用基础和 reset；关键词页面增加删除、query 编辑、回退及 warning 状态。
- 验证：待执行 `npm run type-check` 与 `npm test`。遗留风险：当前测试以 API/store 边界为主，Element Plus 复杂 DOM 交互仍建议在可写 Vite 临时目录的环境补跑组件级测试。

## 2026-08-27 — 拆分前端功能组件并统一 API 错误拦截

- 修改：将 `App.vue` 的关键词优化、图片识别、历史记录和设置拆为独立组件；新增统一 JSON 请求封装，按网络、校验、服务端和未知错误分类显示，并统一复用请求格式。
- 验证：`npm run type-check` 通过；Vitest 因受限环境无法写入 Vite 临时目录未启动。遗留风险：当前图片识别仍为本地 Mock，后续接入后端时复用请求错误分类。

## 2026-08-27 — 新增第一版关键词优化服务

- 修改：将搜索辅助关键词逻辑移入可替换的规则/字典 provider 与 `KeywordOptimizeService`，支持中英文、混合输入、去重、空输入和 fallback 降级；响应统一使用 `keyword` 并兼容 `SearchAssistResponse`。
- 验证：新增关键词建议与 provider 失败降级测试；不调用真实 AI API。注意：词典仍是最小规则集，平台链接仍为固定跳转模板。

## 2026-08-27 — 增加 MVP 统一搜索辅助 API 契约

- 修改：新增版本化 `POST /api/v1/search/assist` Pydantic 契约，返回规则 Mock 英文关键词、三平台跳转链接、实际 query 及降级信息；同步 API 文档和后端测试。
- 验证：已执行后端 pytest 及前端类型检查；真实图库和 AI 未调用。注意：平台 URL 与关键词 provider 仍为生产替换点。

> 所有开发者和自动化 Agent 在修改仓库前必须完整阅读本文件；修改完成后在本说明下方、旧记录上方追加最新记录。保持简短、具体，不记录任何密钥或敏感数据。

## 2026-08-26 — 创建 CreatingEasy MVP monorepo

- 修改：新增 Vue 3 + TypeScript + Vite 的 Manifest V3 插件，完成文本、识图、历史、设置四个约 380px Popup Tab 与本地 mock 交互；新增 FastAPI 健康检查、文本优化和图片分析 mock API，使用 Pydantic 契约并校验图片类型、大小与文件签名；补齐根 README、环境变量示例和产品/API/路线图文档。
- 验证：前端 `npm run type-check`、3 个 Vitest 测试和 `npm run build` 通过，产物包含 `dist/manifest.json`；后端 `ruff check --no-cache .` 通过，6 个 pytest API 用例均显示通过。
- 注意：本机 Python 3.14.3 下 pytest 在输出 6 个通过标记后未自动退出，需在项目目标 Python 3.11 环境复核退出行为；前端全量引入 Element Plus 导致主 JS 约 1 MB，后续可按需引入优化体积。当前前端明确不连接后端，历史仅保存在当前 Popup 会话。

## 2026-08-26 — 初始化项目 harness

- 修改：新增根级 `AGENTS.md`，明确产品目标、功能优先级、技术边界、开发约束、测试要求与完成定义；建立本修改记录作为强制开发记忆。
- 验证：人工核对规则与 `paintingEasy需求文档.docx` 的结构化文本内容一致，并检查两个 Markdown 文件可正常读取。
- 注意：仓库当前尚无应用代码或可执行测试；需求文档因本机缺少 LibreOffice 未完成页面渲染，仅完成结构化内容提取。
