# PaintingEasy 修改记录

## 2026-09-11 — 精简知识架构并保留外部 API 扩展边界

- 修改：移除 Wikidata、ConceptNet 的实现、配置、SQLite 专用缓存/并发编排、冒烟测试和相关测试；删除旧手工关键词知识库、旧三平台规则、知识固化工具及孤立 `appservicesag.py`。职业规则的通用双语类型迁入领域模型，平台偏好只从 `shared/platforms.json` 读取。
- 修改：腾讯中文词向量成为唯一关键词知识提供器，并通过依赖注入使用有界 TTL/LRU 进程内缓存，缓存键仅保存查询摘要；保留旧 `network_expansion` 请求字段兼容但不再触发公共知识请求。
- 修改：新增通用外部 JSON API 客户端，保留可注入 HTTP 客户端、超时、有限重试和脱敏错误映射；明确保留翻译、结构化大模型、图片语义识别和腾讯词向量四类 Provider 边界。同步前端隐私说明、环境变量、API/部署文档和 Agent 开发约束。
- 验证：后端 43 项 Pytest 通过，本次涉及文件 Ruff 通过；前端类型检查及 21 项 Vitest 通过；真实腾讯模型“咖啡店”首次返回 6 个中文联想词，第二次状态由 `live` 变为 `fresh_cache`；Vite 生产构建通过，仍有既有约 1.06 MB 主分块警告。

## 2026-09-11 — 重排关键词主界面并补齐角色平台

- 修改：将按钮和交互重点色统一为 `#2e4955`，重排为关键词输入优先、结果集中展示的紧凑结构，并在页面底部加入腾讯 AI Lab 中文词向量致谢。
- 修改：关键词改为整块点击多选，移除勾选框和冗长来源说明；平台说明压缩为直接搜索或站内搜索两种简短状态。后端不再过滤小红书、Pixso、创客贴等仅支持站内搜索的平台，角色规则中的全部平台均会返回，无法直达搜索页时打开平台首页。
- 验证：前端类型检查及 21 项 Vitest 通过；后端相关 23 项 Pytest 通过；Vite 生产构建通过。仍有既有约 1.06 MB 主分块警告，未在本次样式任务中拆包。

## 2026-09-11 — 极简化插件界面并压缩结果占用

- 修改：在不改变文本搜索、识图、历史和设置功能顺序的前提下重做前端视觉体系；移除大面积渐变、彩色阴影与重复卡片背景，统一为黑白灰层级、细边框和单一蓝色操作强调。压缩品牌栏、标签栏、表单间距、上传区、历史项和推荐项，让输入框与主操作按钮成为第一视觉焦点。
- 修改：关键词结果改为固定高度双列紧凑卡片，平台结果改为固定高度单列列表，两区超量时内部滚动，保留全部选择、删除、理由、许可和登录信息但不再无限撑长弹窗；选中状态使用轻量背景和边框反馈。
- 验证：前端类型检查、21 项 Vitest 和生产构建通过；使用本地真实页面分别检查空状态与“暖色咖啡店宣传海报”结果状态，确认导航、输入、双列关键词、单列平台及内部滚动均正常，无内容遮挡。构建仍有既有主 JS 约 1.06 MB 分块提示，不影响输出。

## 2026-09-11 — 中文化推荐说明并调整平台列表布局

- 修改：关键词卡片不再直接展示 `core`、`expanded`、`scene` 等内部英文枚举，统一显示简短中文分组、分类和来源；将原本只在悬停中可见的推荐理由直接展示，并精简原始词、词典转换和职业排序说明。移除结果区的 `Mock` 英文角标，改为“可自由调整”。
- 修改：素材平台选择区由多列按钮改为单列横向条目，一个平台占一行；平台名称、简短用途、许可状态和登录要求分层排列，长用途单行省略，完整许可仍可悬停查看。补充组件回归断言。
- 验证：后端全量 88 项测试通过，涉及文件 Ruff 通过；前端类型检查及全量 21 项 Vitest 通过，生产构建通过。构建主 JS 为 1,062.47 kB（gzip 345.64 kB），仍触发 Vite 的 500 kB 提示但不影响构建。本轮真实复测 Wikidata Action API 3 次均返回 HTTP 403 和机器人策略提示，当前运行网络仍不可用。

## 2026-09-11 — 使用腾讯轻量中文词向量替换 ConceptNet

- 修改：下载并校验 `light_Tencent_AILab_ChineseEmbedding.bin`（143,613 词、200 维、116,021,049 字节，SHA-256 `5515923c7e67cdc7eb42996546e0bad273c8452f3bfad6db0794e51c848d151b`）到后端本地模型目录；新增基于 NumPy 的惰性加载中文联想 Provider，支持短语优先、中文子词回退、余弦相似度阈值、稳定 Top-N、非中文过滤和模型缺失安全降级。模型文件不进入 Git，原始 query 始终保留。
- 修改：ConceptNet 默认关闭，本地腾讯词向量默认启用且不受“关闭网络扩展”影响；Wikidata 保留为可选网络 Provider。同步配置、API 类型、界面隐私提示与部署文档。因 Python 3.14 下 `gensim` 无预编译包且需要额外 C++ 工具链，改用有兼容 wheel 的 NumPy，避免引入 `text2vec`、PyTorch 和 Transformer 依赖。
- 验证：真实模型首次加载约 0.38 秒；“咖啡店”稳定输出“咖啡馆”(0.9340)、“咖啡厅”(0.9078)、“咖啡屋”(0.8734)、“甜品店”(0.8347)、“面包店”(0.7682)、“书店”(0.7472)。后端全量 88 项测试通过，涉及文件 Ruff 通过；前端类型检查、21 项 Vitest 和生产构建通过。保留既有约 1.06 MB 前端分块警告；腾讯轻量词表对未登录词会安全返回空候选。

## 2026-09-10 — 启用真实公共知识网络并记录部署风险

- 修改：Wikidata 与 ConceptNet 运行默认开关改为启用，搜索路由正式装配 SQLite 缓存、双 Provider 并发可靠性编排和官方 HTTPS 地址；公共服务无需 API key，统一发送可识别 `PaintingEasy/0.1 knowledge-expansion (local deployment)` User-Agent。新增兼容默认值为 `true` 的 `network_expansion` 请求字段，部署环境仍可分别关闭 Provider；插件首次基础优化前说明会发送当前搜索词，设置可持久关闭网络扩展，图片、历史和密钥不发送，前端埋点不再携带完整 query。
- 验证：独立真实冒烟以固定非敏感词分别请求官方服务 5 次。目标网络中 Wikidata 为 HTTP 403 × 5、P95 490.9 ms，ConceptNet 为 HTTP 502 × 5、P95 1390.2 ms；两者超时、HTTP 429 和 `Retry-After` 均为 0。完整搜索 API 对真实请求如实返回双 `original_fallback`，未绕过限制或用 Mock 冒充成功；明确标记的脱敏 fixture 另行验证双 `live`、双 `fresh_cache`、`live + original_fallback` 部分成功、`stale_cache` 和双 `original_fallback` 状态。冒烟脚本不进入 pytest，详细结果见 `docs/deployment.md`。
- 验证：后端全量 87 项测试均运行至通过状态（既有 Windows/Python 3.14 pytest 完成后不退出，已终止空闲进程），涉及文件 Ruff `--no-cache` 通过；前端类型检查及全量 21 项 Vitest 通过，生产构建通过。构建仍有既有主包约 1.06 MB 的分块警告。部署风险：当前目标网络无法成功访问任一官方服务，上线环境必须复测；在此之前系统依赖合格旧缓存或原始 query 安全回退。

## 2026-09-10 — 结构化大模型智能优化

- 修改：新增可替换的 `LanguageModelProvider` 严格契约和 Provider 内 JSON 解析边界；默认结构化 Mock 只在 `smart` 模式执行。受控 Prompt 仅携带原始描述、语言、职业上下文、标准化知识候选、允许的平台规则与数量限制，不记录或返回 Prompt/原始响应；模型结果严格校验核心意图、最多 6 个核心词、8 个扩展词、`derived_from`、原因、不确定项及最多 5 个有序白名单平台，拒绝 URL、额外字段、超量、重复和无依据新主体，最终 URL 仍由平台连接器生成。
- 修改：加入线程安全的可注入额度计数与 `AI_PROVIDER`/`AI_SMART_QUOTA` 环境配置；非法 JSON、结构错误、未知平台、虚构主体、超时和额度不足均恢复完整基础候选。API 保留旧字段并新增核心意图、不确定项和 `generation_status.remaining_uses`；前端仅展示剩余次数和是否已降级，不接收密钥。`AI_PROVIDER_API_KEY` 仍只由环境设置读取，本阶段没有真实 Provider 或公网调用。
- 验证：涉及 ContextBuilder、Provider、搜索模式、候选模型、额度配置、路由和测试的 Ruff `--no-cache` 检查通过；后端全量 85 项用例均运行至通过状态（既有 Windows/Python 3.14 pytest 完成后不退出，已终止空闲进程）；前端类型检查和全量 19 项 Vitest 通过。覆盖结构化边界、数量约束、基础模式零调用、额度耗尽、失败降级、平台排序、原义保护及 `derived_from` 透传；同步更新 API 文档和环境变量示例。

## 2026-09-10 — 职业平台配置与版权状态

- 修改：将五类职业的平台初稿统一迁入 `shared/platforms.json` 的 `persona-platforms-v1` 版本配置，补齐花瓣网、小红书、Pinterest、Pixso、创客贴、Behance、Freepik、Pixiv、ArtStation、Unsplash、Pexels、视觉中国、Pixabay、Figma Community、Dribbble；每项集中记录职业优先级、擅长类型、语言、登录要求、推荐原因、版本和 URL 能力。前后端职业排序、设置选项及推荐展示均从共享配置生成；平台顺序仍标记为 `pending_user_review`，等待用户审核时只需调整配置。
- 修改：新增六种版权状态及简短非法律保证标签；灵感平台明确不能默认下载商用，许可随资源、账号或订阅变化的平台提示逐项核对。没有确认稳定 query URL 的小红书、Pixso、创客贴不再伪造链接，标记为需要站内操作；旧 `platforms` 可执行链接及既有字段继续保留，新元数据以兼容默认值扩展。
- 验证：涉及平台配置、API 模型、职业规则、URL 生成和相关测试的 Ruff `--no-cache` 检查通过；后端全量 76 项用例均运行至通过状态（当前 Windows/Python 3.14 环境在 pytest 完成后未自动退出，已终止空闲进程）；前端类型检查及全量 18 项 Vitest 通过。新增覆盖配置完整性、职业稳定排序、无稳定 URL、登录状态、版权提示与展示；未发起真实公网或付费 API 请求。

## 2026-09-10 — 移除有限本地词库的默认在线召回

- 修改：`KeywordRetriever` 默认不再装配手工概念表，运行时及搜索路由显式使用无本地知识模式；`KeywordOptimizeService` 也不再默认启用猫、咖啡、城市等字典 Provider。旧知识库、`LocalKnowledgeExpansionProvider` 和规则类完整保留，可通过 `use_local_knowledge=True` 或显式注入供历史测试与离线固化工具使用。
- 修改：搜索始终把规范化原始 query 作为不可丢失的核心候选，再合并去重后的 Wikidata、ConceptNet 或缓存候选；外部候选标记为 `external`，检索策略使用 `external`/`hybrid`，两源无候选时使用 `fallback`，不再误报 `rules_only`。全失败仍保留中文、英文或混合原始语义并生成平台 query/URL；职业条件词、职业排序和平台固定规则继续独立生效。
- 验证：后端全量 pytest 通过（73 tests，1 条既有 Starlette 422 常量弃用警告）；涉及本地知识库、RAG、优化服务、职业与平台规则、运行装配、路由及兼容测试的 Ruff `--no-cache` 检查通过。覆盖未见外部新词、外部去重、两源全失败、中文/英文/混合规范化、默认不命中有限概念表、职业/平台规则和旧响应字段兼容；真实联网仍关闭。

## 2026-09-10 — 实现双知识 Provider 并发与独立降级

- 修改：Wikidata 与 ConceptNet 现在按 Provider 独立执行“新鲜缓存 → 实时请求 → 合格旧缓存”，只有新鲜缓存未命中的 Provider 才进入线程池并发请求；单方成功结果不会被失败方丢弃，双方失败时分别合并旧缓存，全部无候选时统一标记 `original_fallback`。结果按 Provider 配置顺序稳定输出，主等待时间取决于最慢一方而非两方耗时相加。
- 修改：可靠性编排保留独立限流、有限重试、熔断和半开探测，并将同 Provider、同 query 的请求合并为一个在途任务，后续请求复用首个结果；熔断、限流、超时或实时失败后仍可读取旧缓存。对外来源状态严格限定为 `fresh_cache`、`live`、`stale_cache`、`original_fallback`，warning 仅含脱敏摘要；搜索服务同步透传各来源状态。
- 验证：后端全量 pytest 通过（66 tests，1 条既有 Starlette 422 常量弃用警告）；涉及 Provider、编排、缓存、搜索服务、契约和测试文件的 Ruff `--no-cache` 检查通过。mock transport 覆盖双成功、部分成功、双方失败、有/无旧缓存、新鲜缓存独立命中、请求风暴合并、并发耗时、超时、熔断半开恢复和稳定顺序；真实联网保持关闭。

## 2026-09-10 — 增加新鲜缓存与受控旧缓存容灾

- 修改：SQLite 知识缓存新增明确的 `get_fresh`/`get_stale` 接口并保留旧 `get` 兼容入口；schema 原位迁移加入 `validated`、`stale_until`、`last_accessed_at`、schema/关系规则版本和启用状态，既有未过期记录继续可用，清理延后至超过容灾截止时间。读取时重新执行 Pydantic、provider 版本、关系白名单、启用和最长时限校验，损坏或不兼容数据一律视为未命中。
- 修改：Provider 调整为“新鲜缓存 → 实时请求 → 合格旧缓存”，成功、空结果、失败分别使用可配置 TTL，失败不覆盖仍可容灾的成功记录；SQLite 初始化、读写或清理失败均不阻塞 Provider 主路径。缓存继续仅保存 query 摘要和标准化候选，并将 `derived_from` 脱敏为固定摘要标记；补充 ConceptNet `IsA` 关系兼容。
- 验证：后端全量 pytest 通过（61 tests，1 条既有 Starlette 422 常量弃用警告）；涉及缓存、Provider、契约、配置和测试文件的 Ruff `--no-cache` 检查通过。覆盖新鲜命中、过期未命中、旧缓存回退、最长时限、旧表迁移、版本/规则/禁用失效、损坏数据库与记录、并发读写、访问时间、三类 TTL 和隐私字段；真实联网开关仍关闭，未改变搜索 API 结果。

## 2026-09-10 — 建立基础/智能搜索模式兼容契约

- 修改：搜索请求新增默认值为 `basic` 的 `optimization_mode`，响应保留 `query`、`suggestions`、`platforms` 等旧字段并新增模式、知识来源状态、最多五项平台推荐和结构化生成状态；基础模式不调用大模型，智能模式在本阶段明确回退基础结果且不消耗 AI 额度。运行入口固定使用本地检索，未访问公网。
- 修改：前端搜索流程要求先选身份、再选基础或智能模式，并展示额度说明；Pinia 保存身份、模式和生成状态。补齐旧请求序列化、模式切换、空输入与无大模型调用回归测试；同时保留无稳定 ID 旧建议的删除能力及自定义平台链接。
- 验证：后端全量 pytest 通过（53 tests，1 条既有 Starlette 422 常量弃用警告）；涉及文件 Ruff `--no-cache` 检查通过；前端类型检查通过；Vitest 17 项全部通过。智能生成、Wikidata 与 ConceptNet 公网调用仍按后续阶段门禁保持禁用。

## 2026-09-10 — 更新搜索模式与外部知识源实施约束

- 修改：增量更新 `AGENTS.md`，明确基础/智能优化分流、Wikidata 与 ConceptNet 并发调用、新鲜缓存与受控旧缓存降级、取消有限本地词库默认召回、五类职业平台初稿、版权状态和最终真实联网门禁；保留既有兼容、安全和测试约束。新增分 7 阶段执行的 Codex 提示词，要求前六阶段离线验收完成后再启用两个官方公网连接并记录真实连通性，禁止用 Mock 冒充成功。
- 验证：人工核对新增要求与本轮确认的搜索链路一致；本次仅修改 Markdown 文档，未运行代码测试。职业平台顺序仍待用户审核，真实联网状态须在执行提示词最后阶段验证。

## 2026-09-10 — 完成可选外部知识扩展链路

- 修改：新增统一 `KnowledgeExpansionProvider` 契约并将现有知识库检索适配为默认本地 Provider；加入仅保存查询摘要和标准化候选的 SQLite 缓存、默认关闭的 Wikidata 中英文标签/别名 Provider、ConceptNet 安全关系白名单 Provider，以及共享的超时配置、有限重试、并发限制、请求合并和熔断编排。旧搜索响应字段与默认 `rules_only` 行为保持不变，外部调用在线程池执行且失败只返回脱敏降级提示。
- 修改：新增候选离线固化服务和 dry-run 脚本，按使用频次、保留/删除率、来源、关系、多语言字段、平台范围及人工审核状态生成稳定 ID 的版本化候选；未显式 `--approve` 不写知识库，重复执行幂等，可通过 `enabled=false` 禁用或回滚。
- 验证：本次涉及文件 Ruff 检查通过；后端全量 pytest 通过（50 tests，1 条既有 Starlette 422 常量弃用警告）；前端类型检查通过。前端 Vitest 在放开 Vite 临时目录写入后运行 16 项，其中 15 项通过，1 项既有关键词删除同步测试失败（期望删除 `warm`，实际仍保留），本次未修改前端逻辑。Python `compileall` 因工作区不允许写入 `__pycache__` 未完成，但后端测试已成功导入并执行新增模块。
- 注意：Wikidata 和 ConceptNet 均为可选网络扩展，默认关闭；ConceptNet 当前只做关系级白名单和语言/格式过滤，投入生产前仍需补充面向业务的敏感词审核策略与许可证复核。SQLite 缓存文件已加入忽略规则，不保存完整 query、Prompt、密钥、原始响应或敏感 URL。

## 2026-09-10 — 补充本地文档与依赖环境忽略规则

- 修改：`.gitignore` 新增需求文档、Python 虚拟环境、安装产物和 `*.egg-info` 忽略规则；保留 `pyproject.toml`、`package.json`、`package-lock.json` 等依赖版本文件可提交。
- 验证：核对忽略规则与仓库现有依赖版本文件路径；未运行代码检查（本次仅修改 Git 忽略配置和开发记录）。

## 2026-09-04 — 最终验收知识库与响应模型

- 修改：完成五种职业规则、六平台共享配置、代表性关键词分类、候选分组/溯源、平台独立 query/URL、确定性评分和旧字段兼容视图验收；修正默认 Embedding provider 为未启用，默认明确返回 `rules_only`，避免将 Mock 误报为向量检索。未接入真实 Embedding、LLM、FAISS 或外部 API，未发现密钥或敏感日志。
- 验证：职业/平台/分类审计通过；平台测试 3 项通过；服务冒烟验证通过；前端 `npm run type-check` 通过；Ruff 执行但仓库存在 105 项既有格式/导入告警；完整后端 pytest 因环境缺少 `PIL` 无法收集；前端 Vitest 因无法写入 `extension/node_modules/.vite-temp` 无法启动。
- 未解决：后端完整 API/图片测试和前端组件测试仍需可安装 Pillow 且允许 Vite 临时目录写入的环境复核。下一步接入真实 Embedding 前需确定模型/provider 配置、超时配额、脱敏 fixture、向量版本迁移和稳定排序基线。

## 2026-09-04 — 增加检索元信息和兼容层

- 修改：新增 `RetrievalMeta` 与新搜索响应字段，明确 rules-only、hybrid、fallback 策略、Embedding 状态、知识库版本、候选数和降级含义；旧 `query`、`suggestions`、`platforms` 保留为兼容视图并由新候选结果生成。同步前端类型与 API 示例，预留 v2 迁移空间。
- 验证：后端平台测试 3 项通过；直接服务冒烟验证通过；前端 `npm run type-check` 通过。完整后端 pytest 仍受环境缺少 `PIL` 依赖影响，Vitest/生产构建仍受 `.vite-temp` 写入限制影响。未返回 Prompt、向量内容或供应商异常详情。

## 2026-09-04 — 实现平台独立响应

- 修改：新增 `PlatformQuery` 领域模型，返回平台独立 terms、各分组候选 ID、登录状态、确定性评分和采用规则原因。平台 query 不再直接拼接全部候选词；核心词按选中状态保留，平台规则隔离，URL 从最终 terms 重新生成。同步前端平台类型并保留现有界面布局。
- 验证：平台配置测试与前端类型检查待本次执行；完整后端 pytest 可能受当前环境缺少 `PIL` 依赖影响，Vitest/构建可能受 `.vite-temp` 写入限制影响。未调用外部图库或 AI 服务。

## 2026-09-04 — 重构关键词响应领域模型

- 修改：新增经范围校验的 `ScoreBreakdown` 与确定性加权 `final_score`，新增带稳定 ID、分组、来源、溯源和平台适用范围的 `KeywordCandidate`；文本与图片响应统一使用新候选模型。前端删除操作改用 `id`，保留旧测试数据的类型兼容，不改变界面布局；同步 API 文档。
- 验证：前端 `npm run type-check` 通过；新增平台模型测试 3 项通过。完整后端 pytest 因当前环境缺少 `PIL` 依赖未完成收集；Vitest/生产构建因受限环境无法写入 `.vite-temp` 未启动。未暴露供应商响应、Prompt 或密钥。

## 2026-09-04 — 统一六个平台配置与 query 规则

- 修改：新增 `shared/platforms.json` 作为文本搜索、图片搜索和推荐平台共用配置源，覆盖 Unsplash、Pexels、Pixabay、Freepik、视觉中国和花瓣网；补齐平台能力、语言、规则原因、query 策略、登录标记、启用状态和版本。后端按最终 terms 独立生成平台 query 与 URL，保留核心词并限制词数；前端图片平台、文本搜索、关键词删除后的链接均改为从配置重建 URL。
- 验证：`extension` 的 `npm run type-check` 通过；新增平台配置单元测试并更新六平台 API 断言。后端 pytest 因当前环境缺少 `PIL` 依赖未完成收集；Vitest 因受限环境无法写入 `extension/node_modules/.vite-temp` 未启动。未调用外部图库或 AI 服务。

## 2026-09-04 — 补齐结构化职业排序规则

- 修改：新增严格校验的职业配置和独立规则引擎，覆盖平面设计师、插画师、摄影师、电商工作者与 UI 设计师的双语名称、目标、分类权重、条件修饰词、禁止扩展、平台优先级、提示上下文和版本。职业仅对候选词做可解释稳定排序；修饰词必须命中显式条件，UI 扩展额外要求 UI 意图，关系禁用不影响直接匹配主体，未选择职业时不生成职业词。关键词优化服务未硬编码职业规则，未接入 LLM 或外部服务。
- 验证：后端完整 pytest 通过（36 tests，1 条既有 Starlette 弃用警告）；本次涉及文件 Ruff 检查通过。全仓既有 Ruff 告警未在本次扩大范围处理。

## 2026-09-04 — 结构化关键词知识库

- 修改：以严格 Pydantic 模型实现 `KeywordConcept`、双语标准词/别名/检索词和带权概念关系，使用稳定 ID、版本、启停及适用范围字段；七个主要分类均补充多条示例。检索改为仅消费校验后的知识库，按概念 ID 去重、忽略禁用项，并禁止关系扩展引入未直接匹配的主体。
- 验证：后端完整 pytest 通过（26 tests，1 条既有 Starlette 弃用警告）；本次涉及文件 Ruff 检查通过。全仓 Ruff 仍有 108 项既有格式/导入告警，集中在旧 API、provider、分析脚本和遗留 `appservicesag.py`；未引入数据库、FAISS、真实 Embedding 或外部 API。

## 2026-09-04 — 扩展识图搜索平台与职业推荐

- 修改：识图搜索平台由 2 个扩展至 Unsplash、Pexels、Pixabay、Freepik、视觉中国和花瓣网，对需登录站点明确标示；识图页底部新增按现有五类职业切换的网站推荐栏，未选择职业时展示通用推荐。未修改图片识别、关键词筛选和文本搜索逻辑。
- 验证：前端类型检查通过；16 个 Vitest 测试通过；生产构建通过。构建仍有既有主包大于 500 kB 警告；视觉中国与花瓣网的登录状态由目标网站自行处理。

## 2026-09-03 — 统一文本与识图关键词结果界面

- 修改：移除文本关键词结果的重复卡片嵌套和图片页重复的识别按钮；文本与识图统一复用同一关键词编辑、筛选、平台单选及搜索结果组件，图片删除关键词时同步更新完整关键词。
- 验证：前端类型检查通过；14 个 Vitest 测试通过；生产构建通过。构建仍有既有主包大于 500 kB 警告。

## 2026-09-03 — 复用文本与识图关键词选择组件

- 修改：抽取共享关键词选择组件，文本优化与图片识图统一复用完整关键词框、关键词多选、平台单选和搜索按钮；图片识别结果支持删除、编辑和按选中词跳转。
- 验证：待执行前端类型检查；未新增外部依赖。

## 2026-09-03 — 修复上传目录不存在导致的启动错误

- 修改：应用启动阶段主动创建图片上传目录后再挂载静态文件服务，避免 `backend/uploads` 不存在时触发运行时错误。
- 验证：已完成 Python 语法级检查；建议重启后端后访问 `/health` 与图片上传接口。

## 2026-09-03 — 修复上传目录启动错误

- 修改：FastAPI 启动时主动创建图片上传目录，再挂载静态文件服务，避免 `backend/uploads` 不存在导致应用启动失败。
- 验证：待执行后端启动与接口测试。

## 2026-09-03 — 增加关键词多选与平台单选搜索

- 修改：平台改为单选，关键词保留完整文本供复制并支持多选；新增底部搜索按钮，跳转仅使用勾选关键词和所选平台，删除关键词同步更新完整文本。
- 验证：待执行前端类型检查与测试。

## 2026-09-03 — 增加请求超时与图片重复上传处理

- 修改：前端请求 15 秒无响应自动结束并显示中文提示，移除错误分类英文前缀；图片选择框每次点击前清空自身值，清空后可再次选择同一文件上传。
- 验证：待执行前端类型检查；后端接口未变更。

## 2026-09-03 — 增加图片职业与平台适配

- 修改：图片区域改为自适应内边距卡片，右下角使用红色“清空”按钮；图片分析接口接收 `persona` 与 `platform`，从本地 JSON 规则组合职业和平台专属关键词，保留对象语义词优先顺序；未来 AI provider 可直接复用结构化上下文。
- 验证：待执行前端类型检查；未引入数据库或真实 AI。

## 2026-09-03 — 修正图片缩略图比例

- 修改：缩略图不再使用固定宽高，改为最大宽高约束并保持 `width/height: auto`，根据图片方向缩放且不改变原始比例。
- 验证：CSS 静态调整；前端类型契约未变化。

## 2026-09-03 — 优化图片预览、手动识别与关键词规则

- 修改：上传后不再自动分析；新增固定尺寸缩略图与屏幕约束原图弹窗；图片关键词优先返回对象语义，并通过本地 JSON 增加平台专属建议与分类，未命中时保留 Mock 降级词；挂载上传静态访问目录。
- 验证：前端类型检查待执行；未接入数据库或真实 AI。

## 2026-09-03 — 增加图片上传与语义识别预留链路

- 修改：新增本地临时图片上传、Pillow 格式/可解析性/大小校验、元数据与过期清理脚本；抽象 `ImageStorageProvider`、`ImageAnalyzeProvider` 并接入稳定 Mock 分析接口；前端支持预览、上传、删除、重新上传和识别按钮。未接入真实 AI API、云存储或以图搜图。
- 验证：待执行后端 pytest、前端类型检查；遗留风险：元数据当前为进程内存，服务重启后历史临时图片不可恢复。

## 2026-09-03 — 增加轻量本地数据埋点

- 修改：新增 `AnalyticsService`、`AnalyticsProvider`、JSONL/Noop provider、埋点上报接口与统计脚本；前端接入搜索、建议生成、图片上传/识别等关键行为，默认脱敏 query。
- 验证：后端 `python -m pytest -q -p no:cacheprovider` 通过（19 tests）；`extension` 的 `npm run type-check` 通过；Vitest 因受限环境无法写入 `extension/node_modules/.vite-temp` 未启动；统计脚本样例运行通过。当前未接入数据库、第三方分析平台或可视化。

## 2026-09-03 — 完善角色选项与关键词联动删除

- 修改：角色提示精简为“您的身份是：”，扩展为五类创作者角色；关键词卡片改为自动换行一次展示，关闭卡片时同步删除建议、最终 query 和平台链接中的对应词。
- 验证：`npm run type-check`、11 个 Vitest 测试和 `npm run build` 通过；构建仅保留既有主包大于 500 kB 警告。未接入埋点、外部 API 或新依赖。遗留风险：新增三类角色当前可透传但尚无专属后端规则，删除统计需后续埋点接入。

## 2026-09-03 — 优化角色选择与关键词标签页交互

- 修改：文本搜索区新增可选用户角色选择框并将 `persona` 传给后端；隐藏原始 query 展示，关键词改为可关闭的标签页，每个标签页展示来源和分类信息。
- 验证：`npm run type-check` 通过；Vitest 因受限环境无法写入 `extension/node_modules/.vite-temp` 未启动。未接入外部 API。遗留风险：角色当前为固定两项，平台选择仍沿用既有设置流程。

## 2026-09-02 — 建立离线 RAG-ready 关键词优化骨架

- 修改：新增可维护关键词知识库、Embedding/LLM 协议与确定性 Mock、规则/别名检索、受控上下文、结构化输出校验和 provider 降级链路；搜索请求增加可选 `persona`/`platforms`，同步前端调用和 API 文档。
- 验证：后端 `python -m pytest -q -p no:cacheprovider` 通过（19 tests）；前端 `npm run type-check` 通过；Ruff 使用 `--no-cache` 检查时受限环境无法写入缓存且现有长行规则仍有告警。未调用外部 API 或本地模型。遗留风险：当前检索仍为规则/别名匹配，真实 Embedding/LLM provider 尚未实现，平台 query 模板尚未做平台级语义改写。

## 2026-08-31 — 修正后端联调入口与跨域配置

- 修改：确认关键词接口为 `POST /api/v1/search/assist`，根路径新增服务说明；开发期启用 CORS，允许前端扩展调用 API；同步接口文档和验证用例。
- 验证：新增根路径与 CORS 预检测试；未调用真实 AI、图库或付费 API。遗留风险：生产环境需将通配 CORS 收紧为实际扩展来源。

## 2026-08-27 — 补齐前端搜索与状态管理自动化测试

- 修改：复用 Vitest、Vue Test Utils 和 jsdom，新增搜索 API mock 边界测试、Pinia 搜索状态测试，并覆盖空输入约束、成功/空结果、错误分类、重试可调用基础和 reset；关键词页面增加删除、query 编辑、回退及 warning 状态。
- 验证：待执行 `npm run type-check` 与 `npm test`。遗留风险：当前测试以 API/store 边界为主，Element Plus 复杂 DOM 交互仍建议在可写 Vite 临时目录的环境补跑组件级测试。

## 2026-08-27 — 拆分前端功能组件并统一 API 错误拦截

- 修改：将 `App.vue` 的关键词优化、图片识别、历史记录和设置拆为独立组件；新增统一 JSON 请求封装，按网络、校验、服务端和未知错误分类显示，并统一复用请求格式。
- 验证：`npm run type-check` 通过；Vitest 因受限环境无法写入 Vite 临时目录未启动。遗留风险：当前图片识别仍为本地 Mock，后续接入后端时复用请求错误分类。

## 2026-08-27 — 新增第一版关键词优化服务

- 修改：将搜索辅助关键词逻辑移入可替换的规则/字典 provider 与 `KeywordOptimizeService`，支持中英文、混合输入、去重、空输入和 fallback 降级；响应统一使用 `keyword` 并兼容 `SearchAssistResponse`。
- 验证：新增关键词建议与 provider 失败降级测试；不调用真实 AI API。注意：词典仍是最小规则集，平台链接仍为固定跳转模板。

## 2026-08-27 — 增加 MVP 统一搜索辅助 API 契约

- 修改：新增版本化 `POST /api/v1/search/assist` Pydantic 契约，返回规则 Mock 英文关键词、三平台跳转链接、实际 query 及降级信息；同步 API 文档和后端测试。
- 验证：已执行后端 pytest 及前端类型检查；真实图库和 AI 未调用。注意：平台 URL 与关键词 provider 仍为生产替换点。

> 所有开发者和自动化 Agent 在修改仓库前必须完整阅读本文件；修改完成后在本说明下方、旧记录上方追加最新记录。保持简短、具体，不记录任何密钥或敏感数据。

## 2026-08-26 — 创建 CreatingEasy MVP monorepo

- 修改：新增 Vue 3 + TypeScript + Vite 的 Manifest V3 插件，完成文本、识图、历史、设置四个约 380px Popup Tab 与本地 mock 交互；新增 FastAPI 健康检查、文本优化和图片分析 mock API，使用 Pydantic 契约并校验图片类型、大小与文件签名；补齐根 README、环境变量示例和产品/API/路线图文档。
- 验证：前端 `npm run type-check`、3 个 Vitest 测试和 `npm run build` 通过，产物包含 `dist/manifest.json`；后端 `ruff check --no-cache .` 通过，6 个 pytest API 用例均显示通过。
- 注意：本机 Python 3.14.3 下 pytest 在输出 6 个通过标记后未自动退出，需在项目目标 Python 3.11 环境复核退出行为；前端全量引入 Element Plus 导致主 JS 约 1 MB，后续可按需引入优化体积。当前前端明确不连接后端，历史仅保存在当前 Popup 会话。

## 2026-08-26 — 初始化项目 harness

- 修改：新增根级 `AGENTS.md`，明确产品目标、功能优先级、技术边界、开发约束、测试要求与完成定义；建立本修改记录作为强制开发记忆。
- 验证：人工核对规则与 `paintingEasy需求文档.docx` 的结构化文本内容一致，并检查两个 Markdown 文件可正常读取。
- 注意：仓库当前尚无应用代码或可执行测试；需求文档因本机缺少 LibreOffice 未完成页面渲染，仅完成结构化内容提取。
